---
interface Props {
  class?: string;
  delay?: number;
}

const { class: className } = Astro.props;
---

<!-- Content is intentionally present and stable without a scroll-observer or JavaScript. -->
<div class:list={['reveal', className]} data-reveal>
  <slot />
</div>
